#!/usr/bin/env sh

set -euo pipefail

cargo build --release --target wasm32-unknown-unknown
mkdir -p demo
cp target/wasm32-unknown-unknown/release/rusty-slider.wasm demo/
cp -pR slides.md theme.json Hack-regular.ttf Amble-Regular.ttf background.png demo/
